const GameConfig = {
    width: 800,
    height: 600,
    fps: 60,
    debug: false,
    
    // Add game-specific configuration here
    difficulty: 1,
    startingLives: 3,
    scoreMultiplier: 1
};

export default GameConfig;